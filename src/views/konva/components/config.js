// const DEFAULT_ROW_HEIGHT = 30; // 默认行高
// const DEFAULT_CELL_WIDTH = 100; // 默认单元格宽度
// const DEFAULT_HEADER_CELL_BG = "#fafafa"; //  header背景色

const defaultConfig = {
  rowHeight: 50, // 默认行高度
  cellWidth: 100, // 默认单元格宽度
  headerBackground: "#fafafa", // 表头背景色
  headerHeight: 50, // 表头高度
  hoverColor: "blue", //#fafafa 表格行悬浮背景色
};

export default defaultConfig;
