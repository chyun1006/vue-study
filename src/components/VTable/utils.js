/**
 * 根据树形结构的数据结构生成表格表头渲染的的行数据
 * 
 */
export const convertTreeColmusToArr = (columns, rows = []) => {
  for (let i = 0; i < columns.length; i++) {
    let row = []
    
  }
}

/**
 * 数组中的每一个对象就是一个单元格
 * [
 *     [{title:'A',colspan:2},{title:'B',colspan:1},{}],
 *     [{},{},{}]
 * ]
 *
 */
